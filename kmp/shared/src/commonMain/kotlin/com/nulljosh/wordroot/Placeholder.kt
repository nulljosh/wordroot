package com.nulljosh.wordroot

// ponytail: scaffold only. Port the real read path (see the web app's
// data layer) into commonMain before wiring the UI to it.
data class PlaceholderItem(val id: String)
